<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SeparatorSectionTitle" parent="StrongSeparatorSection"/>

    <style name="StrongSeparatorSection" parent="SeparatorSection">
        <item name="android:background">@color/black</item>
        <item name="android:layout_height">@dimen/height_line_separator_strong</item>
    </style>

    <style name="SeparatorSection">
        <item name="android:layout_height">@dimen/height_line_separator</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/separator_list_view</item>
    </style>

    <style name="generic_title" >
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">15sp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">start</item>
    </style>



</resources>
